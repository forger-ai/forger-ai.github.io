---
import ScreenshotFrame from './ScreenshotFrame.astro';
import type { Lang } from '../i18n/utils';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;

const copy = {
  en: {
    alt: 'Forger Desktop showing a local app workspace with intelligent agent actions.',
  },
  es: {
    alt: 'Forger Desktop mostrando un espacio local de apps con acciones de agente inteligente.',
  },
}[lang];
---

<ScreenshotFrame
  src="/assets/screenshots/forger-new-experience.png"
  alt={copy.alt}
  class="animate-fade-up delay-300"
/>
