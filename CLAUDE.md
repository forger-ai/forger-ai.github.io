# CLAUDE.md

Read `AGENTS.md` first. It is this repository's Pages-specific agent contract.

Also read `../../AGENTS.md` for workspace rules and cross-repo boundaries.

Use local repository files such as `package.json`, `astro.config.mjs`, `src/`, `public/`, content collections, and workflow files as the implementation source of truth.

Do not duplicate Pages rules here. Update `AGENTS.md` when the contract changes.
